import { ProductcardsDirective } from './productcards.directive';

describe('ProductcardsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductcardsDirective();
    expect(directive).toBeTruthy();
  });
});
